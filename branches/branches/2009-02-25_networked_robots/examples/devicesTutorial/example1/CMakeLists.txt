#
# $Id$
#
# (C) Copyright 2005-2008 Johns Hopkins University (JHU), All Rights
# Reserved.
#
# --- begin cisst license - do not edit ---
# 
# This software is provided "as is" under an open source license, with
# no warranty.  The complete license can be found in license.txt and
# http://www.cisst.org/cisst/license.txt.
# 
# --- end cisst license ---

# name of project and executable
PROJECT(devicesTutorialExample1)

# use CMake provided script to locate FLTK
FIND_PACKAGE(FLTK REQUIRED)

# add include paths
INCLUDE_DIRECTORIES(${devicesTutorialExample1_SOURCE_DIR}
                    ${FLTK_INCLUDE_DIR})

# magic CMake command, generate .cxx and .h from .fl files
# then compile them and link them with the executable
FLTK_WRAP_UI(devicesTutorialExample1 displayUI.fl)
FLTK_WRAP_UI(devicesTutorialExample1GlobalTaskManager displayUIGlobalTaskManager.fl)
FLTK_WRAP_UI(devicesTutorialExample1OmniClient displayUIOmniClient.fl)

#------------------------------------------------------------------------------
#   devicesTutorialExample1
#------------------------------------------------------------------------------
# name the main executable and specifies with source files to use
ADD_EXECUTABLE(devicesTutorialExample1
               # code files to compile
               displayTask.cpp
               main.cpp
               # header files are provided for information
               displayTask.h
               # files generated by fluid
               ${devicesTutorialExample1_FLTK_UI_SRCS}
               )

# this program 
TARGET_LINK_LIBRARIES(devicesTutorialExample1 ${FLTK_LIBRARIES})

# link with the cisst libraries
CISST_REQUIRES("devicesTutorialExample1" "cisstCommon;cisstVector;cisstOSAbstraction;cisstMultiTask;cisstParameterTypes;cisstDevices")

# For apple applications with GUI
IF(APPLE)
  FIND_PROGRAM(APPLE_REZ Rez /Developer/Tools)
  ADD_CUSTOM_COMMAND(TARGET devicesTutorialExample1
                     POST_BUILD
                     COMMAND ${APPLE_REZ}
                     ARGS ${FLTK_INCLUDE_DIR}/FL/mac.r Carbon.r -o ${EXECUTABLE_OUTPUT_PATH}/${CMAKE_CFG_INTDIR}/devicesTutorialExample1)
ENDIF(APPLE)


#------------------------------------------------------------------------------
#   devicesTutorialExample1GlobalTaskManager
#------------------------------------------------------------------------------
# name the main executable and specifies with source files to use
ADD_EXECUTABLE(devicesTutorialExample1GlobalTaskManager
               # code files to compile
               displayTaskGlobalTaskManager.cpp
               omniGlobalTaskManager.cpp
               # header files are provided for information
               displayTaskGlobalTaskManager.h
               # files generated by fluid
               ${devicesTutorialExample1GlobalTaskManager_FLTK_UI_SRCS}
               )

# this program 
TARGET_LINK_LIBRARIES(devicesTutorialExample1GlobalTaskManager ${FLTK_LIBRARIES})

# link with the cisst libraries
CISST_REQUIRES("devicesTutorialExample1GlobalTaskManager" "cisstCommon;cisstVector;cisstOSAbstraction;cisstMultiTask;cisstParameterTypes;cisstDevices")

# For apple applications with GUI
IF(APPLE)
  FIND_PROGRAM(APPLE_REZ Rez /Developer/Tools)
  ADD_CUSTOM_COMMAND(TARGET devicesTutorialExample1GlobalTaskManager
                     POST_BUILD
                     COMMAND ${APPLE_REZ}
                     ARGS ${FLTK_INCLUDE_DIR}/FL/mac.r Carbon.r -o ${EXECUTABLE_OUTPUT_PATH}/${CMAKE_CFG_INTDIR}/devicesTutorialExample1)
ENDIF(APPLE)

#------------------------------------------------------------------------------
#   devicesTutorialExample1OmniServer
#------------------------------------------------------------------------------
# name the main executable and specifies with source files to use
ADD_EXECUTABLE(devicesTutorialExample1OmniServer
               # code files to compile
               displayTaskOmniServer.cpp
               omniServer.cpp
               # header files are provided for information
               displayTaskOmniServer.h
               # files generated by fluid
               ${devicesTutorialExample1_FLTK_UI_SRCS}
               )

# this program 
TARGET_LINK_LIBRARIES(devicesTutorialExample1OmniServer ${FLTK_LIBRARIES})

# link with the cisst libraries
CISST_REQUIRES("devicesTutorialExample1OmniServer" "cisstCommon;cisstVector;cisstOSAbstraction;cisstMultiTask;cisstParameterTypes;cisstDevices")

# For apple applications with GUI
IF(APPLE)
  FIND_PROGRAM(APPLE_REZ Rez /Developer/Tools)
  ADD_CUSTOM_COMMAND(TARGET devicesTutorialExample1OmniServer
                     POST_BUILD
                     COMMAND ${APPLE_REZ}
                     ARGS ${FLTK_INCLUDE_DIR}/FL/mac.r Carbon.r -o ${EXECUTABLE_OUTPUT_PATH}/${CMAKE_CFG_INTDIR}/devicesTutorialExample1)
ENDIF(APPLE)


#------------------------------------------------------------------------------
#   devicesTutorialExample1OmniClient
#------------------------------------------------------------------------------
# name the main executable and specifies with source files to use
ADD_EXECUTABLE(devicesTutorialExample1OmniClient
               # code files to compile
               displayTaskOmniClient.cpp
               omniClient.cpp
               # header files are provided for information
               displayTaskOmniClient.h
               # files generated by fluid
               ${devicesTutorialExample1OmniClient_FLTK_UI_SRCS}
               )

# this program 
TARGET_LINK_LIBRARIES(devicesTutorialExample1OmniClient ${FLTK_LIBRARIES})

# link with the cisst libraries
CISST_REQUIRES("devicesTutorialExample1OmniClient" "cisstCommon;cisstVector;cisstOSAbstraction;cisstMultiTask;cisstParameterTypes;cisstDevices")

# For apple applications with GUI
IF(APPLE)
  FIND_PROGRAM(APPLE_REZ Rez /Developer/Tools)
  ADD_CUSTOM_COMMAND(TARGET devicesTutorialExample1OmniClient
                     POST_BUILD
                     COMMAND ${APPLE_REZ}
                     ARGS ${FLTK_INCLUDE_DIR}/FL/mac.r Carbon.r -o ${EXECUTABLE_OUTPUT_PATH}/${CMAKE_CFG_INTDIR}/devicesTutorialExample1)
ENDIF(APPLE)
